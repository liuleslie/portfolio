Append the following design note to the file `/Users/leslie/Documents/GitHub/portfolio/WISHLIST.md`.

The file uses this format — add a new entry at the bottom with today's date:

```
## YYYY-MM-DD
- <note>
```

If an entry for today already exists, add the note as a new bullet under that date instead of creating a new heading.

The note to log is: $ARGUMENTS
